// BorrowerLoanHistoryController - DISABLED
// Routes moved to BorrowerLoansController (@Get('/history') and @Get('/history/:histId'))
// This file is intentionally empty to avoid routing conflicts with @Get('/:id')
